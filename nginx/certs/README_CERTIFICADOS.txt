En esta carpeta deben quedar estos dos archivos generados con mkcert:

localhost.pem
localhost-key.pem

No subas los certificados ni la llave privada al repositorio.
Para generarlos usa el script:

.\scripts\generar-certificado-local.ps1
