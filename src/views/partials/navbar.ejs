<nav class="sticky top-0 z-50 backdrop-blur border-b border-white/10 bg-slate-950/80">
  <div class="max-w-6xl mx-auto px-4 py-3 flex items-center justify-between gap-4">
    <a href="/" class="flex items-center gap-3 font-bold tracking-tight group">
      <span class="flex h-12 w-12 items-center justify-center rounded-2xl border border-white/10 bg-white/5 overflow-hidden shadow-lg shadow-cyan-950/20">
        <img src="/img/logo.jpeg" alt="Smart Garage" class="h-full w-full object-cover group-hover:scale-105 transition-transform" onerror="this.style.display='none'; this.parentNode.innerHTML='<span class=\'text-cyan-300 text-sm font-black\'>SG</span>'">
      </span>
      <div class="leading-tight">
        <div class="text-lg md:text-xl">Smart Garage</div>
        <div class="text-[11px] text-slate-400 -mt-1">Gestión integral para talleres automotrices</div>
      </div>
    </a>

    <div class="hidden md:flex items-center gap-6 text-sm">
      <a class="hover:text-cyan-300 transition-colors" href="/servicios">Servicios</a>
      <a class="hover:text-cyan-300 transition-colors" href="/nosotros">Nosotros</a>
      <a class="hover:text-cyan-300 transition-colors" href="/busqueda">Búsqueda</a>
      <a class="hover:text-cyan-300 transition-colors" href="/contacto">Contacto</a>
      <a class="hover:text-cyan-300 transition-colors" href="/chat">Chatbot</a>
    </div>

    <div class="flex items-center gap-2 shrink-0">
      <% if (userSession && userSession.role) { %>
        <% if (userSession.role === "ADMIN") { %>
          <a class="px-3 py-2 rounded-xl bg-white/10 hover:bg-white/15 border border-white/10 text-sm" href="/admin/dashboard">Panel Admin</a>
        <% } else { %>
          <a class="px-3 py-2 rounded-xl bg-white/10 hover:bg-white/15 border border-white/10 text-sm" href="/taller/dashboard">Panel Taller</a>
        <% } %>
        <a class="px-3 py-2 rounded-xl bg-rose-500/20 hover:bg-rose-500/30 border border-rose-500/30 text-sm" href="/auth/logout">Salir</a>
      <% } else { %>
        <a class="px-3 py-2 rounded-xl bg-white/10 hover:bg-white/15 border border-white/10 text-sm" href="/auth/login">Iniciar sesión</a>
        <a class="px-3 py-2 rounded-xl bg-gradient-to-r from-cyan-400 to-indigo-500 text-slate-950 font-semibold text-sm" href="/contacto">Solicitar demo</a>
      <% } %>
    </div>
  </div>
</nav>
